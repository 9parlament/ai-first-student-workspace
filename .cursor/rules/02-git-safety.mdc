---
description: Git safety — no commit/push/reset without explicit human OK
globs: "**"
alwaysApply: true
---

# Git

- Не `git commit` / `git push` / `git reset --hard` без явного OK человека.
- Не коммитить `.env`, пароли, `build/`, `allure-results/`.
